const initialState = {
    data:[],
    isLoading: false,
    error: null
}

const todoReducer = (state,action) => {
    switch (action.type){

        default:
            return state
    }
}

export default todoReducer